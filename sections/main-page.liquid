<div class="page-width page-width--narrow section">
  <h1 style="text-align:center; margin-bottom:36px">{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</div>

{% schema %}
{ "name": "Page" }
{% endschema %}
